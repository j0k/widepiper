## Тестовый запуск (testnet) — полный план

Цель: **протестировать весь проект на тестнетах (TRON/BSC/TON)** максимально безопасно и автоматизировано, не задев боевую конфигурацию.

---

## Чек-лист (отмечай по мере выполнения)

| № | Действие | Время | Готово |
|---|----------|-------|--------|
| **Подготовка** |
| 1 | Создать тестнет-кошелёк TRON (Nile), сохранить адрес и приватный ключ | ~15 мин | ☐ |
| 2 | Создать тестнет-кошелёк BSC (MetaMask + сеть Chain ID 97), сохранить адрес и ключ | ~15 мин | ☐ |
| 3 | (Опционально) Создать тестнет-кошелёк TON, сохранить адрес и mnemonic | ~10 мин | ☐ |
| 4 | Получить тестовые TRX (nileex.io) и BNB (testnet.binance.org/faucet-smart) | 10–30 мин | ☐ |
| **Конфигурация** |
| 5 | `cp .env.testnet.example .env.testnet` и заполнить адреса/ключи/API | ~20 мин | ☐ |
| 6 | `cp .env.example .env` и подставить testnet-значения (RPC, токены, кошельки, SECRET_KEY) | ~20 мин | ☐ |
| 7 | Получить BSC API key для testnet (testnet.bscscan.com) и прописать в .env | ~5 мин | ☐ |
| **Запуск** |
| 8 | Запустить стек: `bash scripts/start-prod-no-docker.sh` | ~5 мин | ☐ |
| 9 | Проверить: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8001/` → 200/302 | 1 мин | ☐ |
| 10 | (При необходимости) Настроить nginx для тестового домена | ~15 мин | ☐ |
| **Функциональные проверки** |
| 11 | Регистрация нового пользователя в webapp | ~5 мин | ☐ |
| 12 | Логин и проверка профиля (кошелёк, usdt_balance = 0) | ~3 мин | ☐ |
| 13 | Отправить тестовый депозит USDT (BSC/TRC20) на DEPOSIT_ADDRESS_* | ~5 мин | ☐ |
| 14 | Дождаться подтверждений, проверить зачисление на usdt_balance (API/UI) | 20–40 мин | ☐ |
| 15 | Создать несколько ставок через UI/API | ~10 мин | ☐ |
| 16 | Дождаться цикла bet_manager (~10 мин), проверить is_winning и начисление выигрыша | ~15 мин | ☐ |
| 17 | Вывод: POST /api/v1/withdraw/ на тестовый адрес, проверить tx в эксплорере и списание баланса | ~15 мин | ☐ |
| 18 | Игры: открыть /games/, сыграть Flappy Bird, проверить game_earn → рост usdt_balance | ~10 мин | ☐ |
| 19 | Telegram: открыть бота, /start или /app, открыть Mini App, логин + баланс + ставка | ~15 мин | ☐ |
| **Автоматическая проверка** |
| 20 | Запустить: `BASE_URL=https://tg.ma8ka.com ./scripts/test-all.sh` — все проверки зелёные | ~2 мин | ☐ |

**Оценка суммарно:** первый полный проход ~3–4 часа; повторный ~1–1.5 часа.

---

## 1. Что именно мы хотим проверить

- **Базовый функционал**
  - Регистрация / логин (web и Telegram Mini App).
  - Просмотр баланса и профиля.
- **Финансы**
  - Депозиты USDT: TRON (TRC-20), BSC (BEP-20), TON (опционально).
  - Начисление депозитов на `UserProfile.usdt_balance`.
  - Ставки, работа bet_manager, начисление выигрыша на баланс.
  - Вывод (withdraw) с тестнет-кошельков.
  - Игры (Flappy Bird) → `game_earn`.
- **Интеграции**
  - Telegram-бот и Mini App.
  - Цена токенов через Moralis/DEX/BscScan c testnet-параметрами.

---

## 2. Подготовка окружения (один раз)

### 2.1. Создать тестнет-кошельки

**TRON (Nile testnet)**
- Установи TronLink.
- Создай новый кошелёк (ТОЛЬКО для тестнета).
- Сохрани:
  - Адрес: `T...`
  - Приватный ключ.

**BSC Testnet**
- MetaMask → добавить сеть BSC Testnet:
  - RPC: `https://data-seed-prebsc-1-s1.binance.org:8545`
  - Chain ID: `97`
  - Explorer: `https://testnet.bscscan.com`
- Создай новый аккаунт (отдельный от прод).
- Сохрани:
  - Адрес: `0x...`
  - Приватный ключ.

**TON Testnet (опционально)**
- Tonkeeper / Tonhub → тестнет-кошелёк.
- Сохрани:
  - Адрес: `EQ...` / `UQ...`
  - 24-слово mnemonic (для `WITHDRAW_TON_MNEMONIC`).

### 2.2. Получить тестовые токены

**TRON (Nile)**
- Получи TRX на `https://nileex.io/join/getJoinPage`.
- Через тестовый DEX обменяй небольшую часть TRX на USDT TRC-20 (если хочешь протестировать TRC20-депозиты).

**BSC Testnet**
- `https://testnet.binance.org/faucet-smart` → получить немного BNB на тестовый адрес.
- Этого достаточно, чтобы гонять тестовые транзакции и, при желании, задеплоить свои mock-токены.

**TON Testnet**
- Используй тестнет-краны / dev-кошельки (по необходимости).

Оценка по времени **(разово)**: 30–60 минут (создание кошельков + краны, зависит от очередей).

---

## 3. Конфигурация `.env` для testnet

### 3.1. Заполнить `.env.testnet`

В корне уже есть шаблон:

```bash
cd /home/jk/space/widepiper
cp .env.testnet.example .env.testnet
```

Заполни в `.env.testnet`:

- `DEPOSIT_ADDRESS_TRC20` / `TRON_PRIVATE_KEY` — тестовые TRON.
- `DEPOSIT_ADDRESS_BSC` / `BSC_PRIVATE_KEY` — тестовые BSC.
- `DEPOSIT_ADDRESS_TON` — тестовый TON (если нужен).
- `BSC_API_KEY` — ключ от `https://testnet.bscscan.com`.

### 3.2. Подготовить `.env` из прод-шаблона, но с testnet-значениями

Система читает именно `.env`, поэтому для тестов делаем чисто тестовую версию:

```bash
cp .env.example .env
```

Дальше в `.env`:

- **SECRET_KEY** — сгенерировать новый (не боевой).
- **WEBAPP_URL** — можно оставить `https://tg.ma8ka.com` (если тестируешь на том же домене), либо временный домен / `https://test.tg.ma8ka.com` + правка nginx.
- **RPC и сети**:
  - `BSC_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545`
  - `PANCAKESWAP_ROUTER_ADDRESS` → использовать адрес из `PANCAKESWAP_ROUTER_ADDRESS_TESTNET` (из `.env.example`).
- **Токены**:
  - `MATTER_TOKEN_ADDRESS` / `IDEA_TOKEN_ADDRESS` — адреса **тестовых** токенов на BSC Testnet (можно задеплоить свои mock-ERC20 или указать любые тестовые токены, которые есть в ликвидности).
- **Кошельки приложения и депозиты**:
  - `APP_WALLET`, `APP_WALLET_PRIVATE_KEY` — тестовый BSC-кошелёк (в котором немного BNB/USDT).
  - `DEPOSIT_ADDRESS_TRC20`, `DEPOSIT_ADDRESS_BSC`, `DEPOSIT_ADDRESS_TON` — скопировать из `.env.testnet`.
- **API ключи**:
  - `MORALIS_API_KEY`, `INFURA_API_KEY`, `BSC_API_KEY`, `TON_API_KEY` — либо отдельные для тестов, либо те же, если провайдеры разрешают.

> Рекомендация: держать один `.env.testnet` как источник правды, а `.env` получать из него простым скриптом-merge.

### 3.3. (Опционально) скрипт переключения `prod` / `testnet`

Можно завести, например, `scripts/use-testnet.sh`:

```bash
#!/usr/bin/env sh
set -e
cd \"$(dirname \"$0\")/..\"

cp .env.testnet .env
echo \"✅ .env переключен на TESTNET\"
```

Аналогично — `scripts/use-prod.sh`, если будет отдельный `.env.prod`.

Оценка по времени: 20–40 минут (заполнить все поля, особенно токены и API).

---

## 4. Старт сервисов на testnet

### 4.1. No-Docker запуск всего стека

Из корня проекта:

```bash
cd /home/jk/space/widepiper
bash scripts/start-prod-no-docker.sh
```

Скрипт:
- создаст/обновит `db/`, `staticfiles/`, симлинки `webapp/db`, `webapp/static`;
- применит миграции и соберёт статику;
- запустит:
  - Django (Gunicorn) на `127.0.0.1:8001`,
  - `telegram_bot`,
  - `bet_manager`,
  - `deposit_monitor` (`manage.py monitor_deposits --interval 60`).

Проверка, что webapp поднялся:

```bash
curl -s -o /dev/null -w \"%{http_code}\\n\" http://127.0.0.1:8001/
```

Ожидается `200` или `302`.

Оценка по времени: 5–10 минут (сборка, миграции, первый запуск).

### 4.2. Nginx (если тестируешь именно по домену)

Если тестируешь **через tg.ma8ka.com**, то nginx уже проксирует на 8001 (мы это настроили ранее).  
Если нужен отдельный тестовый домен — сделай копию nginx-конфига и укажи другой `server_name`.

Оценка по времени: 10–20 минут (если на этом же сервере уже всё стоит).

---

## 5. Сценарии тестирования на testnet (по шагам)

### 5.1. Регистрация, логин и профиль

1. Открой webapp:
   - `https://tg.ma8ka.com` (или тестовый домен / IP).
2. Зарегистрируй нового пользователя.
3. Зайди в профиль, проверь:
   - сгенерированный внутренний crypto-кошелёк,
   - отображение `usdt_balance` (должен быть 0 на старте).

Время: 5–10 минут.

### 5.2. Депозиты

Цель: проверить, что **ончейн-транзакция → DepositTransaction → usdt_balance**.

**BSC Testnet (рекомендуется как основной сценарий):**

1. Отправь небольшой объём тестового USDT BEP-20 на `DEPOSIT_ADDRESS_BSC` из `.env` (тестовый кошелёк).
2. Убедись, что `deposit_monitor` запущен (идёт в лог `Запуск мониторинга депозитов...`).
3. Подожди нужное число подтверждений (по умолчанию 15 для BSC).
4. Проверь:
   - в БД (`DepositTransaction`) или по API `deposit_history`;
   - в UI/через API, что `UserProfile.usdt_balance` увеличился на сумму депозита.

**TRON/TON** — по аналогии, если нужны.

Время (включая подтверждения сети): 20–40 минут (первая транзакция).

### 5.3. Ставки и bet_manager

1. Убедись, что у пользователя есть `usdt_balance > 0` после депозита.
2. Через UI или API создай несколько ставок с разными `bet_percent`.
3. Жди ближайшего запуска `bet_manager` (по умолчанию каждые 10 минут):
   - проверяй `bet_manager.log` (там будут строки `Processing X active bets`, `Winners: ..., Losers: ...`).
4. После цикла:
   - проверь флаг `is_winning` у ставок,
   - проверь изменение `usdt_balance` (победители должны получить `bet_size * 2`).

Время: 15–25 минут (учитывая цикл 10 минут и подготовку ставок).

### 5.4. Вывод средств (withdraw)

1. Подготовь **тестовый** адрес для вывода (TRC20/BSC/TON).
2. Вызови `POST /api/v1/withdraw/` (или через UI, если есть форма):
   - `amount` — небольшая сумма (например `0.1` USDT),
   - `address` — тестовый адрес,
   - `network` — `TRC20` / `BSC` / `TON`.
3. Проверка:
   - в ответе должен быть `success: true`, `tx_id`, `explorer_url`;
   - в сети появляется тестнет-транзакция;
   - `user_profile.usdt_balance` уменьшается на `amount`.

Время: 15–30 минут (включая появление транзакции в эксплорере).

### 5.5. Игры (Flappy Bird) и `game_earn`

1. Открой страницу игр (`/games/`).
2. Сыграй несколько раундов, чтобы что-то заработалось.
3. JS (`flappy-bird.js`) вызовет `POST /api/v1/games/earn/` с суммой:
   - проверь в логах/через API, что `usdt_balance` увеличился;
   - лимит — не более 10 USDT за раз.

Время: 10–15 минут.

### 5.6. Telegram Mini App

1. В `.env` должен быть тестовый `TELEGRAM_BOT_TOKEN` и корректный `WEBAPP_URL` (желательно `https://...`).
2. Убедись, что `telegram_bot` запущен (в no-docker-скрипте он стартует автоматически).
3. В Telegram:
   - открой бота,
   - нажми `/start` или `/app`,
   - открой Mini App (кнопка «Приложение»).
4. Повтори коротко сценарии: логин, просмотр баланса, простая ставка.

Время: 10–20 минут.

---

## 6. Автоматизация проверки (одна команда)

В репозитории уже есть `scripts/test-all.sh`, который:
- гоняет Django-тесты,
- проверяет основные импорты,
- делает простой HTTP smoke-тест (`/`, `/api/v1/get_time`, `/swagger/`, `/api/v1/deposit/addresses/`).

Для **testnet** можно:

```bash
cd /home/jk/space/widepiper
BASE_URL=https://tg.ma8ka.com ./scripts/test-all.sh
```

Это не проверяет реальные депозиты/выводы, но быстро говорит, что сервер жив и API-эндпоинты отвечают.

---

## 7. Итоговая оценка по времени

| Этап | Время |
|------|-------|
| Создание кошельков + краны | 30–60 мин |
| Настройка `.env.testnet` + `.env` | 20–40 мин |
| Первый запуск стека + nginx | 15–30 мин |
| Регистрация + логин + профиль | 5–10 мин |
| Депозит (ожидание подтверждений) | 20–40 мин |
| Ставки + цикл bet_manager | 15–25 мин |
| Вывод | 15–30 мин |
| Игры + `game_earn` | 10–15 мин |
| Telegram Mini App | 10–20 мин |

**Суммарно:** ~**3–4 часа** на первый полный прогон; повторный ~1–1.5 часа.
